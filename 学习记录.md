## 构建网格并且绘制了一个与门，但是这个与门有点不太像
#include <wx/wx.h>

// ========== 1. 自定义画布类，继承自 wxPanel ==========
class Canvas : public wxPanel {
public:
    // 构造函数
    Canvas(wxWindow* parent)
        : wxPanel(parent, wxID_ANY, wxDefaultPosition, wxDefaultSize,
            wxFULL_REPAINT_ON_RESIZE)  // 窗口大小改变时重绘
    {
        // 绑定绘制事件：窗口需要重绘时，调用 OnPaint
        Bind(wxEVT_PAINT, &Canvas::OnPaint, this);
    }

private:
    // 重写 OnPaint：在这里画网格
    void OnPaint(wxPaintEvent& event) {
        wxPaintDC dc(this);          // 创建设备上下文（画笔）

        // 1. 白底
        dc.SetBackground(*wxWHITE_BRUSH);
        dc.Clear();
        //-----网格--------
        // 2. 灰色细线
        dc.SetPen(wxPen(wxColour(220, 220, 220), 1));

        int w = GetSize().x;
        int h = GetSize().y;

        // 竖线：每 20 像素一条
        for (int x = 0; x < w; x += 20) {
            dc.DrawLine(x, 0, x, h);
        }
        // 横线：每 20 像素一条
        for (int y = 0; y < h; y += 20) {
            dc.DrawLine(0, y, w, y);
        }
        int gx = 200;
        int gy = 200;
        int gw = 60;
        int gh = 40;
        
        //门的本体（圆角矩形）
        dc.SetPen(*wxBLACK_PEN);
        dc.SetBrush(*wxWHITE_BRUSH);
        dc.DrawRoundedRectangle(gx, gy, gw, gh, 5);
        //写AND文字
        dc.SetTextForeground(*wxBLACK);
        dc.DrawText("AND", gx + 15, gy + 12);
        // 3.3 画两条输入线（左边）
        int in1_y = gy + gh / 3;       // 第 1 条输入线 y 坐标
        int in2_y = gy + gh * 2 / 3;   // 第 2 条输入线 y 坐标
        dc.DrawLine(gx - 30, in1_y, gx, in1_y);   // 上输入
        dc.DrawLine(gx - 30, in2_y, gx, in2_y);   // 下输入

        // 3.4 画一条输出线（右边）
        int out_y = gy + gh / 2;       // 输出线 y 坐标（正中）
        dc.DrawLine(gx + gw, out_y, gx + gw + 30, out_y);

        // 3.5 在线的端点画小圆点（表示引脚）
        dc.SetBrush(*wxBLACK_BRUSH);
        dc.DrawCircle(gx - 30, in1_y, 3);
        dc.DrawCircle(gx - 30, in2_y, 3);
        dc.DrawCircle(gx + gw + 30, out_y, 3);


    }
};

// ========== 2. 应用程序 ==========
class MyApp : public wxApp {
public:
    virtual bool OnInit() {
        // 创建主窗口
        wxFrame* frame = new wxFrame(NULL, wxID_ANY, "网格画布",
            wxDefaultPosition, wxSize(900, 600));

        // 把 Canvas 放到 frame 里
        new Canvas(frame);

        frame->Show(true);
        return true;
    }
};

wxIMPLEMENT_APP(MyApp);
## step 2 
## 让与门变得能够被鼠标拖动

修改 1：构造函数里多绑 3 个鼠标事件

找到构造函数：

```cpp
Canvas(wxWindow* parent)
    : wxPanel(parent, wxID_ANY, wxDefaultPosition, wxDefaultSize,
        wxFULL_REPAINT_ON_RESIZE)
{
    Bind(wxEVT_PAINT, &Canvas::OnPaint, this);
}
```

改成：

```cpp
Canvas(wxWindow* parent)
    : wxPanel(parent, wxID_ANY, wxDefaultPosition, wxDefaultSize,
        wxFULL_REPAINT_ON_RESIZE)
{
    Bind(wxEVT_PAINT,     &Canvas::OnPaint,     this);
    Bind(wxEVT_LEFT_DOWN, &Canvas::OnLeftDown,  this);  // 新增
    Bind(wxEVT_MOTION,    &Canvas::OnMotion,    this);  // 新增
    Bind(wxEVT_LEFT_UP,   &Canvas::OnLeftUp,    this);  // 新增
}
```

---

## 修改 2：在 `private:` 里加成员变量

原来 `private:` 下面只有 `OnPaint`。现在在 `private:` 后面、`OnPaint` 前面加上成员变量：

```cpp
private:
    // === 门的位置和尺寸（从局部变量升级为成员变量）===
    int m_gateX = 200;
    int m_gateY = 200;
    int m_gateW = 60;
    int m_gateH = 40;

    // === 拖动状态 ===
    bool    m_dragging = false;   // 是否正在拖动
    wxPoint m_dragOffset;         // 鼠标相对门左上角的偏移

    // 重写 OnPaint：在这里画网格
    void OnPaint(wxPaintEvent& event) {
        // ... 原来的代码 ...
    }
```

---

## 修改 3：`OnPaint` 里改用成员变量

**把这段：**
```cpp
int gx = 200;
int gy = 200;
int gw = 60;
int gh = 40;
```

**改成：**
```cpp
int gx = m_gateX;
int gy = m_gateY;
int gw = m_gateW;
int gh = m_gateH;
---

## 修改 4：在 `OnPaint` 后面加 3 个鼠标事件函数

在 `OnPaint` 的 `}` 之后、类的 `};` 之前，加上：

```cpp
    // 鼠标左键按下
    void OnLeftDown(wxMouseEvent& e) {
        wxPoint pt = e.GetPosition();  // 鼠标位置
        wxRect gateRect(m_gateX, m_gateY, m_gateW, m_gateH);
        if (gateRect.Contains(pt)) {
            m_dragging = true;
            m_dragOffset = pt - wxPoint(m_gateX, m_gateY);
            CaptureMouse();  // 捕获鼠标，拖出窗口也不掉
        }
    }

    // 鼠标移动
    void OnMotion(wxMouseEvent& e) {
        if (m_dragging && e.Dragging()) {
            wxPoint pt = e.GetPosition();
            m_gateX = pt.x - m_dragOffset.x;
            m_gateY = pt.y - m_dragOffset.y;
            Refresh();  // 请求重绘
        }
    }

    // 鼠标左键松开
    void OnLeftUp(wxMouseEvent&) {
        if (m_dragging) {
            m_dragging = false;
            if (HasCapture()) ReleaseMouse();
        }
    }
# 拖动闪烁的解决办法

## 原因

你现在用的是 `wxPaintDC`，它**直接在屏幕上画**。

拖动时 `Refresh()` 被疯狂调用，每次重绘都是：

```
1. 系统用背景色擦掉整个窗口   ← 你看到"白屏闪一下"
2. 再重新画网格、门、线       ← 画的过程又闪
```

擦除 + 重画两个步骤被人眼捕捉到，就是**闪烁**。

---

## 解决办法：用双缓冲 `wxAutoBufferedPaintDC`

把 `OnPaint` 里的：

```cpp
wxPaintDC dc(this);
```

改成：

```cpp
wxAutoBufferedPaintDC dc(this);
```

**并在文件顶部加一个头文件：**

```cpp
#include <wx/dcbuffer.h>
```

---

## 原理

`wxAutoBufferedPaintDC` 会：

1. **先在内存里（一个看不见的位图）把整个画面画好**
2. **一次性把内存里的图像贴到屏幕上**

这样人眼永远只看到"最终画面"，中间的擦除、绘制步骤都被藏起来了 → **不闪了**。

```
❌ wxPaintDC:        擦屏幕 → 画网格 → 画门 → 画线   （中间状态可见，闪）
✅ wxAutoBufferedPaintDC:   内存里画好全部 → 一次贴到屏幕   （一步到位，不闪）
```

---

## 改动清单

**第 1 处：文件顶部加头文件**

```cpp
#include <wx/wx.h>
#include <wx/dcbuffer.h>   
```

**第 2 处：`OnPaint` 第一行改**

```cpp
void OnPaint(wxPaintEvent& event) {
    wxAutoBufferedPaintDC dc(this);   // ← 原来是 wxPaintDC
    ...
}
```



## 还有一个配套设置（不加也能用，加了更稳）（好吧试验之后发现必须加，不然报错）

在 `Canvas` 的构造函数里，**加一行**：

```cpp
Canvas(wxWindow* parent)
    : wxPanel(parent, wxID_ANY, wxDefaultPosition, wxDefaultSize,
        wxFULL_REPAINT_ON_RESIZE)
{
    SetBackgroundStyle(wxBG_STYLE_PAINT);   // ← 加这一行
    Bind(wxEVT_PAINT,     &Canvas::OnPaint,     this);
    Bind(wxEVT_LEFT_DOWN, &Canvas::OnLeftDown,  this);
    Bind(wxEVT_MOTION,    &Canvas::OnMotion,    this);
    Bind(wxEVT_LEFT_UP,   &Canvas::OnLeftUp,    this);
}
```

**`SetBackgroundStyle(wxBG_STYLE_PAINT)` 的作用：**

告诉 wxWidgets **"背景不要自动擦，我自己在 OnPaint 里画"**。

---
--
## step3 加一个侧边栏
先加了这几个头文件
#include<wx/sizer.h>
#include<wx/panel.h>
#include<wx/aui/aui.h>
然后改了一下canvas好吧不重要
## step 4点击按钮出现相应的门
